#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;
int main(){
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    int n,q;
    cin>>n>>q;
    int blocksize=(int)ceil(sqrt(n));
    map<int,int> blocks[(int)ceil((double)n/(double)blocksize)];
    for(int i=0;i<n;i++){

    }
    return 0;
}