#include "bits/stdc++.h"
using namespace std;
using ll = long long;
using pii = pair<int,int>;
using pll = pair<ll,ll>;
#define A first
#define B second
template<typename T> int sz(const T &a){return int(a.size());}
vector<pii> overlap(pair<pii,pii> a, pair<pii,pii> b){
    if((a.A.A==a.B.A)==(b.A.A==b.B.A)){

    }
    else{

    }
}
int main(){
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    string a;
    while(cin>>a){

    }
    return 0;
}