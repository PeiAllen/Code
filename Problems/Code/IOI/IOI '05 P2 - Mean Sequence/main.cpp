#include<bits/stdc++.h>

using namespace std;
typedef long long int lli;

int main() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	int n;
	cin>>n;
	lli down=LLONG_MAX;
	lli up=LLONG_MAX;
	int last
	for(int i=0;i<n;i++){

	}

	printf("%lli\n",up+1+down);
	return 0;
}