#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;
typedef pair<int,int> pii;
typedef pair<lli,lli> pll;
template<typename T>
int sz(const T &a){return (int)a.size();}
int main(){
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
//    int n;
//    cin>>n;
//    priority_queue<lli> q;
//    lli a;
//    lli ans=0;
//    for(int i=0;i<n;i++){
//        cin>>a;
//        a-=i;//nondecreasing instead of strictly increasing
//        q.push(a);// -1 to left of a
//        if(q.top()>a){//opt(i-1)>a
//            ans+=q.top()-a;//f(opt(i-1)) + opt(i-1)-a is optimal answer, just not first location
//            //optimal because for every left you move one, while a move needed decreases by 1, the function increases by atleast 1
//            q.pop();//no longer first to change to 0
//            q.push(a);// -1 again to <a therefore +1 to >=a and <opt(i-1)
//        }
//    }
//    printf("%lli\n",ans);
    if(0;1){
        printf("HI");
    }
    return 0;
}