#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;
int main(){
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    int n,k;
    cin>>n>>k;
    lli dp[n+1][k+1];
    memset(dp)
    for(int i=1;i<=n;i++){
		for(int j=0;j<=k;j++){
			dp[i][j]=dp[i-1][]
		}
    }
    return 0;
}