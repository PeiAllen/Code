#include "bits/stdc++.h"
using namespace std;
using pii = pair<int, int>;
// __int128 x;

int main() {
    int n;
    vector<int> arr(100, 32);
    // vector<pii> arr3 = {{2, 3}, {1, 5}};
    // map<pii, pii> arr2 = {{{2, 3}, {1, 2}}, {{2, 5}, {1, 3}}};
    // map<int, pii> arr5 = {{1, {2, 3}}};
    cin >> n;
    for (int i = 0; i < n; i++) {
        // printf("%d\n", __gcd(i, int(x)));
        // x = 3;
    }
}